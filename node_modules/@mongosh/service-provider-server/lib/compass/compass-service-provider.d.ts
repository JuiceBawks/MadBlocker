/// <reference types="node" />
import CliServiceProvider from '../cli-service-provider';
import { MongoClient } from 'mongodb';
import { ReplPlatform, MongoClientOptions } from '@mongosh/service-provider-core';
import ConnectionString from 'mongodb-connection-string-url';
import { EventEmitter } from 'events';
interface DataService {
    client: {
        client: MongoClient;
    };
}
declare class CompassServiceProvider extends CliServiceProvider {
    readonly platform: ReplPlatform;
    constructor(mongoClient: MongoClient, bus: EventEmitter, driverOptions?: MongoClientOptions, uri?: ConnectionString);
    static fromDataService(dataService: DataService): CompassServiceProvider;
}
export default CompassServiceProvider;
